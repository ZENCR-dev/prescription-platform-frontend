# =============================================================================
# PRESCRIPTION PLATFORM FRONTEND - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# 
# This file serves as a template for environment variables required by the
# Traditional Chinese Medicine Prescription Platform frontend application.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit .env.local to version control (it's gitignored by default)
# 4. Restart your development server after making changes
#
# SECURITY NOTE: 
# - Only variables prefixed with NEXT_PUBLIC_ are accessible in the browser
# - Server-side only variables should NOT have the NEXT_PUBLIC_ prefix
# - All sensitive values must be kept in .env.local, never in this template
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# These variables connect your Next.js app to your Supabase project
# Find these values in your Supabase dashboard: Settings > API

# Supabase Project URL - Your unique project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon Key - Public API key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key - Private key for server-side operations
# WARNING: Keep this secret! Only use in server-side code
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION  
# -----------------------------------------------------------------------------

# Application Base URL - Used for redirects, sitemap generation, etc.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Application Environment - Affects behavior and feature flags
NODE_ENV=development

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -----------------------------------------------------------------------------

# NextAuth Secret - Used for JWT token signing and encryption
# Generate a secure secret: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-nextauth-secret-here

# NextAuth URL - The canonical URL of your site (used for callbacks)
NEXTAUTH_URL=http://localhost:3000

# Guest Mode Configuration - Enable/disable guest access features
NEXT_PUBLIC_ENABLE_GUEST_MODE=true

# -----------------------------------------------------------------------------
# MEDICAL DATA & COMPLIANCE
# -----------------------------------------------------------------------------

# TCM Database Configuration - Traditional Chinese Medicine data source
TCM_DATABASE_URL=your-tcm-database-connection-string

# GDPR/HIPAA Compliance Mode - Enable additional privacy protections
PRIVACY_COMPLIANCE_MODE=strict

# Data Retention Period (in days) - How long to retain prescription data
DATA_RETENTION_DAYS=90

# -----------------------------------------------------------------------------
# THIRD-PARTY INTEGRATIONS
# -----------------------------------------------------------------------------

# Analytics Configuration
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=your-ga-measurement-id

# Error Tracking (Optional)
SENTRY_DSN=your-sentry-dsn-here
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Image Optimization & CDN (Optional)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name

# -----------------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING
# -----------------------------------------------------------------------------

# Enable detailed logging in development
DEBUG_MODE=false

# Enable SQL query logging (development only)
LOG_DATABASE_QUERIES=false

# Mock API responses (useful for development without backend)
USE_MOCK_API=false

# -----------------------------------------------------------------------------
# PRODUCTION OVERRIDES
# -----------------------------------------------------------------------------
# These variables should be overridden in production deployments

# Production Database URL (override in production)
# DATABASE_URL=your-production-database-url

# Production API Base URL (override in production)  
# NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com

# Production CDN URL (override in production)
# NEXT_PUBLIC_CDN_URL=https://cdn.your-domain.com

# =============================================================================
# END OF CONFIGURATION TEMPLATE
# =============================================================================
#
# NEXT STEPS AFTER COPYING TO .env.local:
# 1. Replace all placeholder values with your actual configuration
# 2. Remove any variables you don't need for your deployment
# 3. Add any additional environment variables specific to your setup
# 4. Run `npm run build` to verify all required variables are properly set
# 5. Test your application thoroughly before deploying to production
#
# For more information on Next.js environment variables:
# https://nextjs.org/docs/app/building-your-application/configuring/environment-variables
# =============================================================================